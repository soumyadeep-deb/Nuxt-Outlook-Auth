{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "NODE_OPTIONS='--max-http-header-size=16384' nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@sidebase/nuxt-auth": "^0.4.4",
    "nuxt": "^3.3.2"
  },
  "dependencies": {
    "jwt-decode": "^3.1.2",
    "next-auth": "^4.18.8"
  }
}
